package bloomfilter

import org.junit.Test
import java.io.File
import kotlin.test.assertEquals
import kotlin.test.assertTrue

class PlotTest {
/*
    @Test
    fun createPlot(){
        val fileName = "testPlot"
        val results = arrayOf(
                Result(100, 5, 0,100, 93, 10, 10),
                Result(200, 5, 0,42, 93, 10, 10),
                Result(300, 5, 0,59, 93, 10, 10),
                Result(500, 5, 0,90, 81, 10, 10)
        )
        Plotter.plotResults(results, fileName, "length")
        val file = File("/data/" + "$fileName" + "_length_precision.png")
        assertTrue(file.delete())
        val file2 = File("/data/" + "$fileName" + "_length_matching_time.png")
        assertTrue(file2.delete())
        val file3 = File("/data/" + "$fileName" + "_length_computing_time.png")
        assertTrue(file3.delete())
    }

    @Test
    fun createPlotForRealData(){
        val fileName = "realDataTestPlot"
        val results = arrayOf(
                Result(100, 5, 0,1359, 1272, 10, 10),
                Result(300, 5, 0,1206, 1205, 10, 10),
                Result(600, 10, 0,1204, 1204,10, 10),
                Result(900, 15, 0,1204, 1204, 10, 10)
        )
        Plotter.plotResults(results, fileName, "length")
        val file = File("/data/" + "$fileName" + "_length_precision.png")
        assertTrue(file.delete())
        val file2 = File("/data/" + "$fileName" + "_length_matching_time.png")
        assertTrue(file2.delete())
        val file3 = File("/data/" + "$fileName" + "_length_computing_time.png")
        assertTrue(file3.delete())
    }*/
}